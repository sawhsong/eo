<?xml version="1.0" encoding="UTF-8"?>
<project name="ZZZ - Create Deployment Set" default="copy" basedir=".">
	<property file="makeDeployment.properties"/>

	<target name="copy" description="Copy war">
		<mkdir dir="${det.war}"/>
		<copy todir="${det.war}">
			<fileset file="${src.war}/${war.fileName}"/>
		</copy>
	</target>
</project>